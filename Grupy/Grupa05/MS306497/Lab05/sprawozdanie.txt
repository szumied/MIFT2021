0. Zapoznano sie z diagramem omawianym na zajeciach

1. Zapoznano sie z oprogramowaniem Jenkinsa

2. Stworzono kontener dockerowy z Jenkinsem

2.1 docker network create jenkins

2.2 docker run \
  --name jenkins-docker \
  --rm \
  --detach \
  --privileged \
  --network jenkins \
  --network-alias docker \
  --env DOCKER_TLS_CERTDIR=/certs \
  --volume jenkins-docker-certs:/certs/client \
  --volume jenkins-data:/var/jenkins_home \
  --publish 2376:2376 \
  docker:dind \
  --storage-driver overlay2

2.3 Dockerfile
FROM jenkins/jenkins:2.277.1-lts-jdk11
USER root
RUN apt-get update && apt-get install -y apt-transport-https \
       ca-certificates curl gnupg2 \
       software-properties-common
RUN curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add -
RUN apt-key fingerprint 0EBFCD88
RUN add-apt-repository \
       "deb [arch=amd64] https://download.docker.com/linux/debian \
       $(lsb_release -cs) stable"
RUN apt-get update && apt-get install -y docker-ce-cli
USER jenkins
RUN jenkins-plugin-cli --plugins "blueocean:1.24.5 docker-workflow:1.26"

2.4 docker build -t myjenkins-blueocean:1.1 .

2.5 docker run \
  --name jenkins-blueocean \
  --rm \
  --detach \
  --network jenkins \
  --env DOCKER_HOST=tcp://docker:2376 \
  --env DOCKER_CERT_PATH=/certs/client \
  --env DOCKER_TLS_VERIFY=1 \
  --publish 8080:8080 \
  --publish 50000:50000 \
  --volume jenkins-data:/var/jenkins_home \
  --volume jenkins-docker-certs:/certs/client:ro \
  myjenkins-blueocean:1.1
  
3. Wrzucono obraz swojego Jenkinsa na DockerHub'a
sudo docker tag 03786fce472e mserwaczak/lab05
sudo docker push mserwaczak/lab05

4. Z poziomu Jenkinsa uruchomionego w kontenerze uruchomiono docker-compose stworzony na Lab04
- Jenkins uruchomiony w kontenerze
- po Lab04 docker-compose.yml jest w repozytorium
- Z poziomu Jenkinsa sciagnieto repo gdzie jest plik docker-compose.yml
- Jenkins uruchamia plik docker-compose.yml tworzac srodowisko z Lab04


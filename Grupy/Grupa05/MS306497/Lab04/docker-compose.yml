version: '3.3'
services:
 build:
  container_name: docker-build-nodechat
  image: build:latest
  build:
   context: .
   dockerfile: Dockerfile-build
 test:
  container_name: docker-test-nodechat
  image: test:latest
  build:
   context: .
   dockerfile: Dockerfile-test
  depends_on:
  - build

Bartek Ciesiński Metodyki DevOps, Lab05, 15.04.2021

Zad.0	Zapoznałem się z diagramem omawianym na zajęciach.

Zad.1	Zapoznałem się z oprogramowaniem Jenkins.

Zad.2	Stworzyłem kontener dockerowy z Jenkinsem zgodnie z instrukcją:
	 * sudo docker network create jenkins
	 * sudo docker run \
 		 --name jenkins-docker \
 		 --rm \
 		 --detach \
		 --privileged \
 		 --network jenkins \
 		 --network-alias docker \
 		 --env DOCKER_TLS_CERTDIR=/certs \
 		 --volume jenkins-docker-certs:/certs/client \
 		 --volume jenkins-data:/var/jenkins_home \
 		 --publish 2376:2376 \
 		 docker:dind \
 		 --storage-driver overlay2
	Następnie stworzyłem Dockerfile i zbudowałem obraz:
	 * sudo docker build -t myjenkins-blueocean:1.1 .
	Uruchomiłem kontener:
	 * sudo docker run \
 		 --name jenkins-blueocean \
 		 --rm \
 		 --detach \
 		 --network jenkins \
 		 --env DOCKER_HOST=tcp://docker:2376 \
 		 --env DOCKER_CERT_PATH=/certs/client \
 		 --env DOCKER_TLS_VERIFY=1 \
 		 --publish 8080:8080 \
 		 --publish 50000:50000 \
 		 --volume jenkins-data:/var/jenkins_home \
 		 --volume jenkins-docker-certs:/certs/client:ro \
 		 myjenkins-blueocean:1.1 
 		 
zad.3 	Wrzuciłem obraz utworzonego w punkcie 2 Jenkinsa na DockerHub'a:
	 * sudo docker tag 401d83c1d458 ciesinskib/lab_05
	 * sudo docker push ciesinskib/lab_05

zad.4 	Otworzyłem w Przeglądarce Jenkinsa, Skonfigurowałem projekt i uruchomiłem.
	Projket zbudował się prawidłowo.



version: '3'
services:
    build-agent:
      image: image-node
      build: Dockerfile
      ports:
      - 3000:3000
      stdin_open: true
      tty: true
      
    test-agent:
      image: image-test
      build: Dockerfile_test
      ports:
      - 3030:3030
      depends_on:
      - build-agent
     
      
      
      

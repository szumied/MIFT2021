1) Stworzylem swojego brancha Grupa07-KK306667-Lab03
2) Hook znalazl sie w odpowiednim miejscu - MIFT2021/Grupy/Grupa07/KK306667/Lab03/hooks/pre-commit
3) Nastpnie zainstalowalem dockera przy pomocy komend
	-sudo apt-get update
	
	-sudo apt-get install \
    	apt-transport-https \
    	ca-certificates \
    	curl \
    	gnupg \
    	lsb-release
    	
	- curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
	
	-echo \
  "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
  
  	-apt-get update
  	
  	-apt-get install docker-ce docker-ce-cli containerd.io
  	
4)Zarejestrowalem swoje konto na stronie dockera
5)Stworzyem plik Dockerfile i umiescilem go w folderze Lab02(znajduje sie takze w folderze 03)2

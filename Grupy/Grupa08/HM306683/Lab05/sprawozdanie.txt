2.
docker network create jenkins
Utworzenie mostu sieciowego w Dockerze.

docker run --name jenkins-docker --rm --detach \
  --privileged --network jenkins --network-alias docker \
  --env DOCKER_TLS_CERTDIR=/certs \
  --volume jenkins-docker-certs:/certs/client \
  --volume jenkins-data:/var/jenkins_home \
  --publish 2376:2376 docker:dind --storage-driver overlay2
Pobranie i uruchomienie obrazu Dockera docker:dind.

docker build -t myjenkins-blueocean:1.1 .
Zbudowanie obrazu z wczesniej utworzonego Dockerfile.

docker run --name jenkins-blueocean --rm --detach \
  --network jenkins --env DOCKER_HOST=tcp://docker:2376 \
  --env DOCKER_CERT_PATH=/certs/client --env DOCKER_TLS_VERIFY=1 \
  --publish 8080:8080 --publish 50000:50000 \
  --volume jenkins-data:/var/jenkins_home \
  --volume jenkins-docker-certs:/certs/client:ro \
  myjenkins-blueocean:1.1
Uruchomienie obrazu myjenkins-blueocean:1.1 jako kontener.

docker exec jenkins-blueocean cat /var/jenkins_home/secrets/initialAdminPassword
Wydrukowanie hasla potrzebnego do oblokowania Jenkinsa.

3.
docker push hubertmurawka/myjenkins-blueocean
Umieszczenie obrazu w DockerHubie.
  
4.
  docker run \
  --name jenkins-blueocean \
  --rm \
  --detach \
  --network jenkins \
  --user root \
  --env DOCKER_HOST=unix:///var/run/docker.sock \
  --env DOCKER_CERT_PATH=/certs/client \
  --publish 8080:8080 \
  --publish 50000:50000 \
  --volume jenkins-data:/var/jenkins_home \
  --volume jenkins-docker-certs:/certs/client:ro \
  --volume /var/run/docker.sock:/var/run/docker.sock \
  myjenkins-blueocean:1.1
Uruchomienie obrazu myjenkins-blueocean:1.1 jako root oraz zdefiniowanie zmiennych srodowiskowych.

1. Stworzenie brancha osobistego
	git checkout -b Nazwa

2. Dodanie githooka 
	cp sciezka/commit-msg .
	git add commit-msg

3. Instalacja Dockera
	sudo yum install -y yum-utils
	sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
	sudo yum install docker-ce docker-ce-cli containerd.io

4. Zalozenie konta

5. Stworzenie i wypelnienie dockerfile
	touch
Dodanie 
	git add

6.Uruchomienie kontenera i sciagniecie repo

	sudo docker build - tworzymy kontener
	sudo docker run -it nazwa bash

	git clone....
	

Przełączenie na bracha Grupa08, a następnie stworzenie lokalnego brancha:
	-git checkout Grupa08
	-git checkout -b Grupa08-MW306701_Lab03
Dodanie git hook'a do nowo utworzeonego folderu w hooks w katalogu lab02:
	-mkdir hooks
	-touch pre-commit
Docker'a miałem już zainstalowanego na mojej wirtualnej maszynie
Utworzyłem nowe konto na stronie Docker Hub
Storzenie Dockerfile
Zbudowanie obrazu za pomocą komendy:
	-sudo docker built -t nazwa obrazu .
Uruchomienie kontenera za pomocą:
	-sudo docker run -it nazwa obrazu bash
Sklonowanie w bashu repozytoriu za pomocą:
	-git clone repo
Sprawdzenie czy istnieje nasz osobistyy branch i wlasciwe katalogi

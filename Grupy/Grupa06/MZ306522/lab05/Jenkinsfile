pipeline {
    agent any 
    environment{
        PATH = "$PATH:/usr/local/bin"
    }
    stages {
        stage('Build') { 
            steps {
                sh '/usr/local/bin/docker-compose up'
		echo 'Build'
            }
        }
    }
}